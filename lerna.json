{
  "version": "0.0.1",
  "npmClient": "yarn",
  "command": {
    "publish": {
      "ignoreChanges": [
        "**/README.md",
        "**/__tests__/**"
      ],
      "message": "chore(release): publish %s [skip ci]"
    }
  },
  "packages": [
    "packages/*"
  ]
}
